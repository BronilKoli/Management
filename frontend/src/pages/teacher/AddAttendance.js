import React from 'react';

const AddAttendance = () => {
    return (
        <div>
            <h1>Add Attendance</h1>
            <p>This is the page where teachers can add attendance.</p>
        </div>
    );
};

export default AddAttendance;